# Remote executor e2e exchange credentials.
# Copy to a local untracked env file and pass it with:
#   python scripts/remote_executor_e2e.py --env-file .env.e2e.local

EXCHANGE_ID=binance
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=
EXCHANGE_API_PASSPHRASE=
